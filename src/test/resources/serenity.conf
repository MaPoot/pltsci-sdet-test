environments {
    default{
        api.hover.url = "http://localhost:8080/v1"
    }
    test{
        api.hover.url = "http://localhost:8080/v1"
    }
}

serenity {
    take.screenshots = AFTER_EACH_STEP
    project.name = Hover Automation Test
    console.colors = true
}
